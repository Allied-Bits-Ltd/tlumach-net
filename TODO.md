* In `Plural` placeholders, add support for internationalized options (one, few, many...)
* Passing of parameters from XAML bindings to TemplatedTranslationUnits
* A version of the library for other languages and frameworks (Python and JavaScript?)
