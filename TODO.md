* Detect and handle .NET format specifiers in numeric placeholders.
* In `Plural` placeholders, add support for internationalized options (one, few, many...)
* Passing of parameters from XAML bindings to TemplatedTranslationUnit instances
* Writer classes to export translations in certain file formats
* A version of the library for other languages and frameworks (JavaScript, Python?)
